name: Foundation validation

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: foundation-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Prepare development environment
        run: cp .env.example .env

      - name: Validate Compose topology
        run: |
          docker compose config >/dev/null
          docker compose -f compose.yaml -f compose.test-noauth.yaml config >/dev/null

      - name: Validate production and operations topologies
        run: |
          docker compose --env-file .env.production.example -f compose.yaml -f compose.production.yaml config >/dev/null
          docker compose --env-file .env.production.example -f compose.yaml -f compose.production.yaml -f compose.keycloak-bootstrap.yaml config >/dev/null
          docker compose --env-file .env.production.example -f compose.yaml -f compose.production.yaml -f compose.ops.yaml --profile ops config >/dev/null

      - uses: pnpm/action-setup@v4
        with:
          version: 10.15.1

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install JS dependencies
        run: pnpm install --frozen-lockfile

      - name: Typecheck protocol package
        run: pnpm --filter @nevolium/protocol typecheck

      - name: Typecheck graph package
        run: pnpm --filter @nevolium/graph typecheck

      - name: Typecheck Gantt package
        run: pnpm --filter @nevolium/gantt typecheck

      - name: Typecheck UI package
        run: pnpm --filter @nevolium/ui typecheck

      - name: Typecheck realtime service
        run: pnpm --filter @nevolium/realtime typecheck

      - name: Typecheck Nevolium Web
        run: pnpm --filter @nevolium/web typecheck

      - name: Build Nevolium Web
        run: pnpm build:web

      - name: Build realtime service
        run: pnpm build:realtime

      - uses: astral-sh/setup-uv@v6

      - name: Prove uv workspace lockfile matches declarations
        run: uv lock --check

      - name: Validate Python packages
        run: |
          uv build --package nevolium-core
          uv build --package nevolium-worker

      - name: Compile Python sources
        run: python -m compileall services/core/src services/worker/src scripts/ops scripts/smoke

      - name: Prove memory Task completion synchronization
        run: python scripts/smoke/memory_projection_wait_contract.py

      - name: Prove deterministic conversational routing
        run: uv run --locked --project services/core python scripts/smoke/assistant_router.py

      - name: Prove Assistant conversation ownership contract
        run: uv run --locked --project services/core python scripts/smoke/assistant_ownership_contract.py

      - name: Prove failed execution timestamps and replay preservation
        run: uv run --locked --project services/core python scripts/smoke/execution_terminal_contract.py

      - name: Prove PydanticAI semantic routing contract
        run: uv run --locked --project services/worker python scripts/smoke/semantic_router_contract.py

      - name: Prove LiteLLM accounting contract
        run: uv run --locked --project services/worker python scripts/smoke/model_gateway_contract.py

      - name: Prove heavy-work timers, lease loss and real memory subprocess ownership
        run: uv run --locked --project services/worker python scripts/smoke/heavy_work_execution_contract.py

      - name: Prove production settings, JWT claims, ops separation and effective topologies
        run: uv run --locked --all-packages python scripts/smoke/deployment_contract.py

      - name: Prove monitored OpenBao token renewal contract
        run: python scripts/smoke/openbao_renewal_contract.py

      - name: Prove bounded production TLS ingress contract
        run: python scripts/smoke/ingress_contract.py

      - name: Prove user-free production Keycloak bootstrap contract
        run: python scripts/smoke/keycloak_production_contract.py

      - name: Prove safe first production Keycloak user provisioning contract
        run: python scripts/smoke/keycloak_first_user_contract.py

      - name: Prove safe nominated Keycloak administrator transition contract
        run: python scripts/smoke/keycloak_nominated_admin_contract.py

      - name: Prove safe Keycloak bootstrap retirement contract
        run: python scripts/smoke/keycloak_bootstrap_retirement_contract.py

      - name: Prove exact Keycloak trusted-proxy configuration contract
        run: python scripts/smoke/keycloak_proxy_contract.py

      - name: Prepare isolated public-address alias for real TLS/DNS proofs
        run: |
          sudo ip addr add 93.184.216.34/32 dev lo
          # Disposable runner only: permit the unprivileged fixture to bind HTTP/TLS ports.
          sudo sysctl -w net.ipv4.ip_unprivileged_port_start=0

      - name: Prove real HTTP/TLS pinning, forbidden destinations and bounded bodies
        run: uv run --locked --all-packages python scripts/smoke/public_web_contract.py

      - name: Prove News Intelligence contract
        run: uv run --locked --project services/worker python scripts/smoke/news_contract.py

  capacity-and-data-integration:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    services:
      postgres:
        image: pgvector/pgvector:0.8.6-pg17@sha256:cf134a767f474095eeba57e0117be8e568e011a63f33fbf252f14c9b760f8e6f
        env:
          POSTGRES_USER: nevolium
          POSTGRES_PASSWORD: nevolium-test-only
          POSTGRES_DB: nevolium_admission_test
        ports: ["5432:5432"]
        options: >-
          --health-cmd "pg_isready -U nevolium -d nevolium_admission_test"
          --health-interval 5s --health-timeout 5s --health-retries 20
    env:
      DATABASE_URL: postgresql+asyncpg://nevolium:nevolium-test-only@localhost:5432/nevolium_admission_test
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v6
      - name: Apply canonical migrations to isolated PostgreSQL
        working-directory: services/core
        run: uv run --locked alembic upgrade head
      - name: Prove concurrent model admission, budgets, expiry and settlement
        run: uv run --locked --project services/core python scripts/smoke/model_admission_contract.py
      - name: Prove shared capacity, bounded reads, rebuild and relay recovery
        run: uv run --locked --project services/core python scripts/smoke/capacity_and_data_contract.py
      - name: Start isolated JetStream for capacity and reconnection validation
        run: >-
          docker run -d --name nevolium-d02-nats -p 127.0.0.1:4222:4222
          nats:2.14.5-alpine@sha256:d4ac35882ac65aff236cd65b9d3fa4d24332c681e1a85f94eedccd3cdd65b1da -js
      - name: Prove real transport limits and existing durable upgrade
        env:
          NATS_URL: nats://127.0.0.1:4222
        run: uv run --locked --all-packages python scripts/smoke/transport_capacity_contract.py
      - name: Clean up isolated JetStream
        if: always()
        run: docker rm -f nevolium-d02-nats || true
      - name: Prove separate production SQL roles and restricted migrations on disposable databases
        run: uv run --locked --project services/core python scripts/smoke/database_privileges_contract.py
      - name: Prove D02 migration rollback and reapply on the disposable database
        working-directory: services/core
        run: |
          uv run --locked alembic downgrade 0012_task_planning
          uv run --locked alembic upgrade head

  command-kernel-integration:
    needs: validate
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - name: Prepare command-kernel environment
        run: |
          cp .env.example .env
          echo 'NEVOLIUM_WORKER_EXTRAS=' >> .env

      - name: Start canonical command and Temporal substrate
        run: |
          docker compose -f compose.yaml -f compose.test-noauth.yaml up -d --build postgres nats seaweedfs temporal-admin-tools temporal temporal-create-namespace nevolium-core

      - name: Prove canonical conversation and command handoff
        run: python scripts/smoke/command_kernel.py

      - name: Dump command-kernel state on failure
        if: failure()
        run: |
          docker compose ps -a
          docker compose logs --no-color --tail=350 postgres nats seaweedfs temporal-admin-tools temporal temporal-create-namespace nevolium-core

      - name: Tear down command-kernel stack
        if: always()
        run: docker compose down -v --remove-orphans

  semantic-command-integration:
    needs: validate
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - name: Prepare semantic-routing environment
        run: |
          cp .env.example .env
          echo 'NEVOLIUM_WORKER_EXTRAS=' >> .env

      - name: Start canonical command, policy and Worker substrate
        run: |
          docker compose -f compose.yaml -f compose.test-noauth.yaml up -d --build postgres nats seaweedfs temporal-admin-tools temporal temporal-create-namespace nevolium-core nevolium-worker

      - name: Start deterministic LiteLLM stand-in
        run: |
          docker run -d --name nevolium-fake-litellm \
            --network nevolium_nevolium \
            --network-alias litellm \
            -v "$PWD/scripts/smoke/fake_litellm.py:/app/server.py:ro" \
            ghcr.io/astral-sh/uv:python3.12-bookworm-slim@sha256:e5b65587bce7de595f299855d7385fe7fca39b8a74baa261ba1b7147afa78e58 python /app/server.py

      - name: Prove durable PydanticAI routing and replay-safe capability handoff
        run: python scripts/smoke/semantic_command.py

      - name: Dump semantic-routing state on failure
        if: failure()
        run: |
          docker compose ps -a
          docker logs nevolium-fake-litellm || true
          docker compose logs --no-color --tail=500 postgres nats temporal-admin-tools temporal temporal-create-namespace nevolium-core nevolium-worker

      - name: Tear down semantic-routing stack
        if: always()
        run: |
          docker rm -f nevolium-fake-litellm || true
          docker compose down -v --remove-orphans

  memory-projection-integration:
    needs: validate
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - name: Prepare memory-projection environment
        run: |
          cp .env.example .env
          echo 'NEVOLIUM_WORKER_EXTRAS=' >> .env
          echo 'NEVOLIUM_MEMORY_PROJECTOR_MODE=stub' >> .env

      - name: Start canonical message, event and Worker substrate
        run: |
          docker compose -f compose.yaml -f compose.test-noauth.yaml up -d --build postgres nats seaweedfs temporal-admin-tools temporal temporal-create-namespace nevolium-core nevolium-worker

      - name: Prove rebuildable memory projection generations
        run: python scripts/smoke/memory_projection.py

      - name: Prove clean Worker SIGTERM after completed projections
        run: |
          worker_id=$(docker compose ps -q nevolium-worker)
          docker compose stop nevolium-worker
          test "$(docker inspect -f '{{.State.ExitCode}}' "$worker_id")" = 0
          test "$(docker inspect -f '{{.State.OOMKilled}}' "$worker_id")" = false

      - name: Dump memory-projection state on failure
        if: failure()
        run: |
          docker compose ps -a
          docker compose logs --no-color --tail=650 postgres nats temporal-admin-tools temporal temporal-create-namespace nevolium-core nevolium-worker

      - name: Tear down memory-projection stack
        if: always()
        run: docker compose down -v --remove-orphans

  block1-integration:
    needs: validate
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4

      - name: Prepare integration environment
        run: |
          cp .env.example .env
          echo 'NEVOLIUM_WORKER_EXTRAS=' >> .env

      - name: Start Block 1 substrate
        run: |
          docker compose -f compose.yaml -f compose.test-noauth.yaml up -d --build postgres nats seaweedfs temporal-admin-tools temporal temporal-create-namespace nevolium-core nevolium-worker

      - name: Prove crash recovery and outbox delivery
        run: python scripts/smoke/block1.py

      - name: Dump service state on failure
        if: failure()
        run: |
          docker compose ps -a
          docker compose logs --no-color --tail=300 postgres nats seaweedfs temporal-admin-tools temporal temporal-create-namespace nevolium-core nevolium-worker

      - name: Tear down integration stack
        if: always()
        run: docker compose down -v --remove-orphans

  resource-integration:
    needs: validate
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4

      - name: Prepare trust-boundary environment
        run: |
          cp .env.example .env
          echo 'NEVOLIUM_WORKER_EXTRAS=' >> .env

      - name: Start identity, secrets, assets and Core substrate
        run: |
          docker compose up -d --build postgres nats seaweedfs temporal-admin-tools temporal temporal-create-namespace openbao keycloak nevolium-core

      - name: Prove authenticated resource boundaries
        run: python scripts/smoke/resources.py

      - name: Dump trust-boundary state on failure
        if: failure()
        run: |
          docker compose ps -a
          docker compose logs --no-color --tail=400 postgres nats seaweedfs temporal-admin-tools temporal temporal-create-namespace openbao keycloak nevolium-core

      - name: Tear down integration stack
        if: always()
        run: docker compose down -v --remove-orphans

  backup-restore-integration:
    needs: validate
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - name: Prepare disaster-recovery environment
        run: |
          cp .env.example .env
          cat >> .env <<'EOF'
          RESTIC_REPOSITORY=/repository
          RESTIC_PASSWORD=nevolium-ci-restic-proof
          RESTIC_LOCAL_PATH=./backups/restic
          EOF

      - name: Prove Restic backup and destructive restore
        run: bash scripts/smoke/backup_restore.sh

      - name: Dump recovery state on failure
        if: failure()
        run: |
          docker compose ps -a
          docker compose logs --no-color --tail=250 postgres nats seaweedfs

      - name: Tear down recovery stack
        if: always()
        run: |
          docker compose --env-file .env -f compose.yaml -f compose.override.yaml -f compose.ops.yaml --profile ops run --rm ops-cleanup || true
          docker compose --env-file .env -f compose.yaml -f compose.override.yaml -f compose.ops.yaml --profile ops down -v --remove-orphans
          rm -rf backups/restic .nevolium-backup-staging

  block2-policy-integration:
    needs: validate
    runs-on: ubuntu-latest
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v4

      - name: Prepare autonomy-boundary environment
        run: |
          cp .env.example .env
          echo 'NEVOLIUM_WORKER_EXTRAS=' >> .env

      - name: Start durable execution, identity and Core policy substrate
        run: |
          docker compose up -d --build postgres nats seaweedfs temporal-admin-tools temporal temporal-create-namespace keycloak nevolium-core nevolium-worker

      - name: Prove approval suspension, hard budgets and policy tokens
        run: python scripts/smoke/block2_policy.py

      - name: Dump autonomy-boundary state on failure
        if: failure()
        run: |
          docker compose ps -a
          docker compose logs --no-color --tail=450 postgres nats temporal-admin-tools temporal temporal-create-namespace keycloak nevolium-core nevolium-worker

      - name: Tear down autonomy stack
        if: always()
        run: docker compose down -v --remove-orphans
