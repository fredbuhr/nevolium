[project]
name = "nevolium-workspace"
version = "0.1.0"
requires-python = ">=3.12"

[tool.uv.workspace]
members = ["services/core", "services/worker"]

[tool.ruff]
target-version = "py312"
line-length = 100
