# Nevolium Worker

Temporal worker and AI execution process.

The dependency set declares the permanent execution engines from the beginning: PydanticAI, Mem0, Graphiti, Docling, Browser Use and Playwright. The first executable workflow is deliberately deterministic; agent/tool activities are added only after the Core policy/approval contract exists.
